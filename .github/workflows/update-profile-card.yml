name: Update live contribution card

on:
  schedule:
    - cron: "17 5 * * *"
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/update-profile-card.yml"
      - "scripts/generate-activity-card.mjs"

permissions:
  contents: write

jobs:
  update-card:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Retrieve public contribution calendar
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api graphql -f query='query { user(login: "pablo2611") { contributionsCollection { contributionCalendar { totalContributions weeks { firstDay contributionDays { date weekday contributionCount contributionLevel } } } } } }' > calendar.json
      - name: Render activity card
        run: node scripts/generate-activity-card.mjs calendar.json assets/activity-heatmap.svg
      - name: Commit changed card
        run: |
          git diff --quiet || (git config user.name "github-actions[bot]" && git config user.email "41898282+github-actions[bot]@users.noreply.github.com" && git add assets/activity-heatmap.svg README.md && git commit -m "Update live contribution card" && git push)
